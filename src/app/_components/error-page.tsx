"use client";
import {ErrorPage, type ErrorPageProps } from "@maany_shr/rage-ui-kit"

export const ErrorPageClientPage  = (props: ErrorPageProps ) =>{
    return (
        <ErrorPage {...props}/>
    )
}